# LAN Screen Stream

Открытое приложение для передачи экрана и системного звука с компьютера на телефон, планшет, телевизор или другой компьютер через браузер в одной локальной сети.

## Цель

- минимально возможная задержка в LAN;
- передача видео через WebRTC без перекодирования на сервере;
- системный звук воспроизводится на устройстве-приёмнике;
- просмотр без установки клиента — достаточно браузера;
- 1080p/60 FPS как базовая цель, далее 1440p/4K;
- один передатчик и несколько приёмников.

> Полностью нулевая задержка физически невозможна. Практическая цель — стабильная задержка порядка десятков миллисекунд в хорошей проводной или Wi‑Fi 6 сети.

## Архитектура MVP

```text
Компьютер-передатчик
  ├─ getDisplayMedia: экран + системный звук
  ├─ WebRTC peer connection для каждого зрителя
  └─ WebSocket: только обмен SDP/ICE

Локальный Node.js-сервер без внешних зависимостей
  ├─ раздаёт веб-интерфейс
  └─ выполняет WebSocket-сигналинг, медиапоток через него не проходит

Устройство-приёмник
  └─ браузер принимает WebRTC-видео и звук
```

## Установка одной командой

### Linux и macOS

```bash
curl -fsSL https://raw.githubusercontent.com/3d-mast/lan-screen-stream/main/install | sh
```

### Windows 10/11

```bat
curl.exe -fsSL https://raw.githubusercontent.com/3d-mast/lan-screen-stream/main/install | cmd
```

Обе команды используют один и тот же bootstrap-файл `install`, который выбирает нативный установщик для текущей ОС.

Установщик:

- сам определяет ОС и архитектуру процессора;
- использует установленный Node.js 20+ либо загружает переносимый Node.js 22;
- не требует Git и не выполняет `npm install`;
- создаёт команду `lan-screen-stream`;
- безопасно обновляет уже установленную копию;
- проверяет JavaScript перед завершением установки.

Поддерживаются Linux x64/ARM64/ARMv7, macOS Intel/Apple Silicon и Windows x64/ARM64. На musl-системах, включая Alpine Linux, Node.js устанавливается через `apk`.

После установки запусти:

```bash
lan-screen-stream
```

## Ручной запуск для разработки

Требуется Node.js 20+. Внешних npm-зависимостей нет.

```bash
npm run dev
```

На компьютере-передатчике открыть:

```text
http://localhost:3000
```

На другом устройстве открыть адрес, который сервер покажет в консоли, например:

```text
http://192.168.1.50:3000
```

На компьютере нажать **«Передавать экран»**, затем **«Запустить трансляцию»**. В системном окне браузера включить передачу системного звука. На другом устройстве выбрать **«Смотреть трансляцию»**.

## Важное ограничение браузеров

Захват системного звука зависит от ОС и браузера. Для первого рабочего варианта целевая связка — Windows 10/11 и актуальный Chrome или Edge. При выборе экрана нужно вручную включить пункт передачи системного звука. Некоторые браузеры и Linux/macOS ограничивают захват звука всего рабочего стола.

## Следующие этапы

1. Автоматическое определение передатчика через mDNS/QR-код.
2. Управление битрейтом, FPS, разрешением и кодеком AV1/VP9/H.264.
3. Метрики RTT, потерь пакетов, jitter и фактического битрейта.
4. Режим одного WebRTC-потока через локальный SFU для большого числа зрителей.
5. Аппаратное кодирование и отдельный нативный захват звука там, где браузер ограничен.
6. PIN-код сессии и шифрование служебного обмена.
7. Полноэкранный режим и установка как PWA.

## Лицензия

MIT © 2026 Alexey Prokopchuk
