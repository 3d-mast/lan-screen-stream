{
  "name": "lan-screen-stream",
  "version": "0.1.0",
  "private": false,
  "description": "Low-latency browser screen and system-audio streaming over a local network using WebRTC.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
